# ─── Secrets and credentials ────────────────────────────────────────────
*.key
*.pem
*.crt
*.cer
*.p12
*.pfx
.env
.env.*
!.env.example
credentials.json
credentials
secrets.yaml
secrets.yml
*secret*
*password*
*token*
*.age
*.gpg

# SSH keys
.ssh/id_*
.ssh/*.key
.ssh/known_hosts
.ssh/authorized_keys

# GPG keys
.gnupg/*.key
.gnupg/*~
.gnupg/*.gpg
.gnupg/random_seed

# AWS credentials
.aws/credentials
.aws/config

# GCP credentials
.config/gcloud/credentials*
.config/gcloud/access_tokens.db
.config/gcloud/legacy_credentials

# Other cloud providers
.azure/credentials
.kube/config

# API keys and tokens
.netrc
.npmrc
.yarnrc
.config/gh/
.config/op/
.docker/config.json

# AI / agent tool state (chat history, MCP tokens, session data)
.claude/
.claude.json
.claude.json.backup
.codex/
.cursor/
.config/pi/
.pi/

# History files (may contain sensitive commands)
.*_history
.zsh_history
.bash_history
.python_history
.node_repl_history
.mysql_history
.psql_history

# ─── Local state and noise ──────────────────────────────────────────────
# Local overrides
*.local
shell/private
.envrc

# Backup files
*~
*.bak
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Tmux resurrect files (contain session data)
.tmux/resurrect/
.config/tmux/resurrect/

# Build / cache artifacts
__pycache__/
*.pyc
styles

# Cloned, not vendored
config/nvim/
